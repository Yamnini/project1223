class bank :
    def __init__(self,name,account_no):
        self.name=name
        self.account_no=account_no
    def add_bineficiary(self,name=None,account_no=None):
        if name:
            self.name=name
        if account_no:
            self.account_no=account_no
    def manage_binificiary(self,account_no):
        if account_no=account_no:
            self.account_no=account_no
            
            
